name: workflow-name
on: push

jobs:
  job-name:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
    steps:
      - name: SSH to GitHub Actions
        uses: P3TERX/ssh2actions@v1.0.0
